import {HelloToxin, Input } from '@/components';

const Home = () => {
  return <>
  return <HelloToxin />;
    <Input placeholder="Email" />
    <input type="email" />
    </>;
};

export default Home;
