import { CommentList } from '@/components';

const RoomPage = () => {
  return (
    <div style={{ maxWidth: '712px', margin: '0 auto' }}>
      <CommentList />
    </div>
  );
};

export default RoomPage;
